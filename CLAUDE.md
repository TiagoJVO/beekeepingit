# CLAUDE.md

Entry-point **map** for AI contributors to **BeekeepingIT** (offline-first beekeeping
field app) — so you know where things live and which folders to skip. Operating rules in
`.claude/rules/` are **auto-loaded as memory**; don't import or restate them here.

## Repo map

| Path                | What's there (and when to read it)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `requirements/`     | **Source of truth (intent)** — read when **planning**: context, FRs, NFRs, `decisions.md` (`D-*`), `open-questions.md` (`Q-*`), intended `tech-stack.md`                                                                                                                                                                                                                                                                                                                                                                                                      |
| _GitHub Issues_     | The plan/backlog — epics (`type/epic`) & stories, migrated from the retired `planning/` folder. Scope work via `gh issue list` / the Project board                                                                                                                                                                                                                                                                                                                                                                                                            |
| `docs/`             | Documentation of the system **as built** (+ `adr/`, `CODEMAPS/`) — read to understand existing behavior; `CODEMAPS/` holds token-lean maps for quick orientation. What has landed so far is stated once, in `README.md`'s **Status** line                                                                                                                                                                                                                                                                                                                     |
| `infra/`            | k8s cluster bring-up/teardown + the Helm **umbrella chart** (`EPIC-13`) — see `infra/README.md`; design notes in `docs/architecture/platform.md`                                                                                                                                                                                                                                                                                                                                                                                                              |
| `contracts/`        | Contract-first **OpenAPI specs** per service + the shared sync-validation description (`D-12`, ADR-0003/0025) — the client and services are generated/validated from here; see its own `README.md`                                                                                                                                                                                                                                                                                                                                                            |
| `services/`         | Go backend (`D-5`). `services/shared/` is a **library** (not a deployable service) for cross-cutting infra abstractions — object storage, DB access (`NFR-ARC-2`, `#85`) — see its own `README.md`. `services/servicetemplate/` is the shared service template (health, config, logging, OTel, JWT auth, error format, `#20`) other services bootstrap from — see its own `README.md`; the two are separate Go modules linked by the repo-root `go.work`. Domain services (`identity`, `apiaries`, ...) land per `docs/architecture/service-decomposition.md` |
| `client/`           | Flutter app (`D-5`, `D-10`) — PWA shell, routing, theming, state mgmt, i18n scaffold (`#21`); see its own `README.md`. Slice UI (login, apiary CRUD) lands with `#23`                                                                                                                                                                                                                                                                                                                                                                                         |
| `admin/`            | React + TypeScript **web admin app** (`D-5`, `NFR-ROL-2`) — **online-only** (no offline/PWA), OIDC-authenticated with an admin-role guard (`#72`); see its own `README.md`. Architecture as-built: `docs/architecture/admin-app.md`. Admin screens (members/roles/invitations) land in follow-up M7 stories                                                                                                                                                                                                                                                   |
| `.claude/agents/`   | Subagent definitions — vendored ECC reviewers/resolvers plus this repo's own `contracts-reviewer` and `infra-reviewer`; see its own `README.md` for which to use when                                                                                                                                                                                                                                                                                                                                                                                         |
| `.claude/commands/` | Project slash commands — the `/orch-*` orchestration surface, `/orch-review`, and `/update-codemaps`                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `.claude/rules/`    | Operating rules — already auto-loaded, no need to open                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `.claude/skills/`   | Project skills (procedural how-tos), e.g. `requirements-folder` — invoked on demand; open the `SKILL.md` when doing that task                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `.github/`          | Issue/PR templates, `labels.yml`, CI workflows                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `scripts/`          | Dev scripts: bootstrap, git-hook checks, and the `check-*.sh` lint gates `task lint` runs (deploy-URL drift, authentik mappings, web-image hardening, repo map)                                                                                                                                                                                                                                                                                                                                                                                               |
| `taskfiles/`        | Per-language task definitions included by the root `Taskfile.yml` (`repo`, `go`, `web`, `dart`, `openapi`) — one `lint` / `format` / `test` / `build` surface                                                                                                                                                                                                                                                                                                                                                                                                 |
| `CONTRIBUTING.md`   | Branching, commits, PR process — read when committing / opening a PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

### Notes (lifecycle — not derivable from the tree)

- **Backlog lives in GitHub** — the former `planning/` folder was a temporary Markdown
  stand-in; it has been **migrated to GitHub Issues + Projects and retired**. Scope work
  items there (`gh issue list`), not from a repo folder.
- **Directories are created as work needs them** — nothing is pre-scaffolded. **Every
  top-level directory must have a row in this map and a line in `README.md`'s layout tree**,
  added in the same PR that creates it (per `definition-of-done.md`). `task lint` enforces
  this via `scripts/check-repo-map.sh`, so a missing row fails CI rather than drifting.
