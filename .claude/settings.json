{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "permissions": {
    "allow": [
      "Read",
      "Grep",
      "Glob",
      "Bash(git status*)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(git show*)",
      "Bash(git branch*)",
      "Bash(go build*)",
      "Bash(go vet*)",
      "Bash(go test*)",
      "Bash(flutter analyze*)",
      "Bash(flutter test*)",
      "Bash(flutter pub get*)",
      "Bash(gh pr view*)",
      "Bash(gh pr list*)",
      "Bash(gh issue view*)",
      "Bash(gh issue list*)",
      "Bash(gh issue create*)",
      "Bash(gh issue comment*)"
    ],
    "deny": [
      "Bash(git push --force*)",
      "Bash(git push -f*)",
      "Bash(git reset --hard*)",
      "Bash(git commit*--no-verify*)",
      "Bash(git commit*-n *)"
    ],
    "ask": ["Edit(.github/workflows)", "Write(.github/workflows)"]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"[BeekeepingIT] Source of truth (intent) is requirements/; docs/ documents what is actually built (M0-M2 platform, services, PWA client and the admin app have landed — current status in README.md). When PLANNING a task, READ requirements/ (context, FRs, NFRs, decisions, open-questions, tech-stack) — at planning, not implementation. As you implement, document the real architecture in docs/ (+ ADRs in docs/adr/). Decisions/requirements are revisitable: if contradicting one makes sense, get user confirmation, then update requirements/. Respect rollout order (D-10) and deferred scope (D-4). FINISHING WORK — when you find out-of-scope work (a bug, a gap, a missing test): do it now if it is small (under ~30 min) and adjacent to the task; otherwise open a GitHub Issue in THIS session (gh issue create, shaped per the backlog-management skill) and link it from the PR; ask the user only when it changes scope or touches a D-*. Never park it in a note for later — a PR is finished when it owes nothing, or every remaining item is an Issue. Full rules: CLAUDE.md + .claude/rules/.\"}}'",
            "statusMessage": "Loading BeekeepingIT workflow rules"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Read|Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f .claude/hooks/secret_file_guard.py ]; then python3 .claude/hooks/secret_file_guard.py 2>/dev/null || python .claude/hooks/secret_file_guard.py 2>/dev/null || echo '{}'; else echo '{}'; fi",
            "statusMessage": "Checking for secret-file access"
          }
        ]
      }
    ]
  }
}
