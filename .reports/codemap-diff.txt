BeekeepingIT — Codemap Analysis Report
Generated: 2026-07-14
Scan: 461 tracked files | branch claude/ecc-update-codemaps-14208a

========================================================================
RUN TYPE: INITIAL BASELINE
========================================================================
No previous docs/CODEMAPS/ existed. This is the first generation, so the
diff is the full inventory (nothing to compare against; no >30% overwrite
gate triggered). Subsequent runs will diff against these five files.

Created:
  docs/CODEMAPS/architecture.md    (~900 tokens)
  docs/CODEMAPS/backend.md         (~1000 tokens)
  docs/CODEMAPS/frontend.md        (~1000 tokens)
  docs/CODEMAPS/data.md            (~950 tokens)
  docs/CODEMAPS/dependencies.md    (~950 tokens)

========================================================================
STRUCTURE SNAPSHOT
========================================================================
Project type : monorepo (Flutter Web PWA client + Go microservices + IaC)
Entry points :
  client/lib/main.dart                 (Flutter app)
  services/{identity,organizations,apiaries,sync}/main.go   (Go services)
  services/servicetemplate/example/main.go   (service template reference)
Source dirs  : client/lib (51 .dart) · services (67 non-test .go) ·
               infra (84) · contracts/openapi (6) · 13 SQL migrations

Services (6 Go modules under go.work):
  identity       — users/profiles                     (owns identity schema)
  organizations  — orgs/memberships/invitations       (owns organizations schema)
  apiaries       — apiaries/counters/conflict+audit    (owns apiaries schema, PostGIS)
  sync           — stateless write-back + token mint    (no DB)
  servicetemplate— shared HTTP/auth/otel scaffold      (library)
  shared         — dbaccess/objectstore/history/devseed (library)

========================================================================
ARCHITECTURE HIGHLIGHTS (baseline)
========================================================================
Client-facing REST routes: 20 across 4 services (see backend.md).
  identity 3 · organizations 8 · apiaries 8 · sync 3
Middleware chain: otelhttp → Recover → RequestID → RequestLogger;
  domain routes behind authn(JWT) → org-resolver → RequireRole.
Sync model: local-first. Client writes → local SQLite (PowerSync CRUD queue)
  → POST /v1/sync/batch → coordinator validate-all-then-apply → owning svc
  /internal/sync/{validate,apply}. Down-sync via PowerSync Sync Rules.
Conflict: last-write-wins by device updated_at; superseded/rejected surfaced
  to user (D-12 notify-and-fix; local sync_rejected_ops dead-letter).
Tenancy: organization_id on every owned row; derived from token server-side.
History: append-only audit_log per schema; DB-enforced immutability.

========================================================================
DEPENDENCIES DETECTED (baseline)
========================================================================
External services : Authentik (OIDC), PowerSync, Postgres+PostGIS (CNPG),
                    MinIO, Traefik (gateway), OTel→Grafana, Flux (GitOps), ghcr.io
Go libs (direct)  : go-chi/chi/v5, jackc/pgx/v5, pressly/goose/v3,
                    minio-go/v7, google/uuid, opentelemetry-go, testcontainers-go
Dart libs (direct): powersync, flutter_riverpod, go_router, openid_client, http,
                    flutter_map, latlong2, geolocator, intl, crypto, uuid, meta, web
Notable inter-service edges: organizations→identity; apiaries→identity,organizations;
                    sync→identity,organizations,apiaries; PowerSync→sync (JWKS).

========================================================================
STALENESS WARNINGS
========================================================================
None — all five codemaps generated fresh on 2026-07-14.
(Future runs: flag any codemap whose Generated date is >90 days old.)

========================================================================
NOTES FOR NEXT RUN
========================================================================
- docs/CODEMAPS/ is a subdirectory of the existing docs/ top-level dir, so no
  CLAUDE.md repo-map / README layout row was required (DoD applies to NEW
  top-level dirs only).
- Only the Apiaries tab is live (M2); Activities/Journeys/Todos/Assistant are
  ComingSoon placeholders (M3–M8). Expect frontend.md route tree to grow.
- Admin App (TypeScript/React, coding-standards.md) not yet in-repo — when it
  lands, add an admin section to frontend.md + a row here.
