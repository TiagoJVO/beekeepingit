- this app is being designed to later scale, but at this point used by a single organization, so feature prioritization should be focused on the features that are most important for a single organization, and not on features that are only relevant for multi-tenant scenarios, not blocking that path, but not prioritizing it. For example, the app should be designed to support multiple organizations, but at this point, the app will only be used by a single organization, so features that are only relevant for multi-tenant scenarios should not be prioritized over features that are relevant for a single organization.
- at this stage, only used in Portugal, so needs to check for portuguese regulations and beekeeping practices of portugal.
