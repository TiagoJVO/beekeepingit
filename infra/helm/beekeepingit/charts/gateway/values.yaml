# Defaults for standalone `helm template`/`helm lint` of this subchart in
# isolation. When installed as part of the umbrella, these are overridden by the
# `gateway:` key in the parent's values.yaml/environments/*.yaml.
enabled: true

# Local dev hostname (k3d maps host 8443 -> 443, see infra/README.md). Add it to
# /etc/hosts (or use `curl --resolve`) to reach it from outside the cluster.
host: keycloak.beekeepingit.local

# The one real backend that exists after #84 — domain services/PWA land later
# (#23). Routes to Keycloak's "http" Service. Keycloak is now its own Flux
# HelmRelease named "keycloak" (ADR-0012), not nested in this umbrella release
# — so the vendored keycloakx chart's fullname is "keycloak-keycloakx-*", not
# "beekeepingit-keycloakx-*". Confirmed via a standalone `helm template` render:
# keycloakx creates both a "-headless" Service, for StatefulSet DNS, and this
# "-http" one, which is the one that actually serves traffic.
backend:
  serviceName: keycloak-keycloakx-http
  servicePort: 80

ingressClassName: traefik
selfSignedCertDays: 365

# Placeholder for standalone rendering only — the umbrella always injects the
# real global.* via its own values.yaml.
global:
  namespace: beekeepingit
